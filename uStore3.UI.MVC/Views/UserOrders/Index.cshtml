@model IEnumerable<uStore3.DATA.EF.UserOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.OrderId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order.OrderId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserOrderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserOrderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserOrderId })
        </td>
    </tr>
}

</table>
